# ChatHub Ver0.2 実装計画

本ドキュメントは v0.2 の要求事項を実装タスクに分解したロードマップ兼進捗管理表。
`plan.md`（v0.0の記録）・`plan_v0.1.md`（v0.1の記録。フェーズA〜F完了、2026-09-18時点で凍結済み）はいずれも編集せず、本ファイルで v0.2 の計画・進捗を管理する。

**【2026-09-18 完結・凍結】** v0.2はタスク1（保留）を除き全タスク完了。本ファイルは以後編集しない過去の記録として扱う。保留にしたタスク1は、単発タスクではなく独立した実装計画として`plan_v0.3.md`に切り出した。以降の開発は`plan_v0.3.md`を唯一の正しいロードマップ（Single Source of Truth）として進める。

進め方のルール:
- 1タスク = 1回の指示で実装・テスト完了できるサイズに分解する。
- タスク着手前に必ずステータスを「進行中」に更新し、完了後は「完了」＋簡単な実施内容・テスト結果を追記する。

ステータス凡例: `未着手` / `進行中` / `完了` / `保留`

---

## フェーズA: 第0章メタデータ取得の仮説検証

### タスク1: OGP/メタタグからのタイトル取得可否の調査（旧 v0.1 タスク14）
- 内容: 「Notionと同様に、タイトルだけであれば OGP/メタタグ情報（`<title>`, `og:title` 等）から取得できるのではないか」という仮説を検証する。ChatGPT/Claude/Gemini の共有ページで実際に取得可能か調査し、現行のスクレイピング処理（Cloudflare Browser Rendering / Jina Reader、`src/app/api/metadata/route.ts`）との比較検討を行う。調査結果次第では、軽量なOGPタグ取得だけで済むページとフル本文スクレイピングが必要なページを判別する分岐実装が発生する可能性がある（実装するかどうかも含めてユーザーと相談する）
- ステータス: 保留
- 保留理由（2026-09-18）: 現状のスコープで着手するには難易度が高いと判断。単発タスクではなく、独立した実装計画として`plan_v0.3.md`に切り出した。次回再開する際は`plan_v0.3.md`を参照すること

---

## フェーズB: パスキー管理UXの改善

### タスク2: パスキーの識別性向上（リネーム機能の追加）
- 内容: 設定画面（`SettingsDialog.tsx`）のパスキー一覧で、PCとスマホなど複数デバイスに登録すると、どちらも認証器（ブラウザ/OSのパスキーマネージャー）が自己申告する`friendly_name`（例: 両方とも「Google Password Manager」）がそのまま表示されるため、どちらがどのデバイスのパスキーか区別できない問題がある。Supabase Authの`supabase.auth.passkey.update({ passkeyId, friendlyName })`（PATCH `/passkeys/{id}`でfriendly_nameを更新するAPI、`node_modules/@supabase/auth-js/src/GoTrueClient.ts`で実装確認済み）を使い、パスキー一覧の各行に編集（リネーム）UIを追加し、ユーザーが任意の名前（例:「自宅PC」「スマホ」）を付けられるようにする
- ステータス: 完了
- 実施内容:
  - `src/lib/constants.ts`に`PASSKEY_NAME_MAX_LENGTH`（20文字）を新設
  - `SettingsDialog.tsx`のパスキー一覧各行に鉛筆（`PencilIcon`）アイコンを追加。クリックで名前表示がインライン入力欄（`FolderPanel.tsx`の改名UIと同様のパターン）に切り替わり、Enterまたは「OK」ボタンで確定
  - 確定時は`supabase.auth.passkey.update({ passkeyId, friendlyName })`を呼び出し、レスポンスで一覧のstateを更新して即座に反映
  - `npm run lint` / `npm run build`: 問題なし。ユーザー本人が実機で「鉛筆アイコンから名前変更→一覧に反映→再読み込み後も保持」を確認済み「問題ありません」

---

## フェーズC: モバイル操作性の改善

### タスク3: モバイルでの一覧行の長押し時に出る意味のない選択状態の解消
- 内容: 一覧テーブルの各行には、ブラウザ版で「行を別の行にドラッグ&ドロップすると2つのアイテムを新しいフォルダにまとめる」機能のため、HTML5ネイティブの`draggable`属性が付いている（`ItemTable.tsx`）。PCではこれによりマウスドラッグが機能するが、モバイル（タッチ操作）ではネイティブDnDのdragイベント自体が発火しない（plan_v0.1.mdタスク6で判明済みの制約と同様）。しかし`draggable`属性自体はブラウザに伝わっているため、モバイルで行を長押しすると「ドラッグ可能な要素です」というブラウザ標準の長押しフィードバック（行の色が沈む＝選択/持ち上げ状態）だけが発生し、その先の操作（実際の移動）につながらない行き止まりの状態になっている。モバイルでは`draggable`を無効化し、この意味のない長押し状態自体を発生させないようにする（PC版のドラッグ機能はそのまま維持。モバイル向けの代替操作の新規実装は本タスクのスコープ外）
- ステータス: 完了
- 実施内容:
  - `ItemTable.tsx`に`window.matchMedia("(pointer: coarse)")`でタッチ操作主体の端末かどうかを検知する処理を追加（`ThemeToggle.tsx`と同様、SSR/クライアントのハイドレーション不一致を避けるためマウント後の`useEffect`で判定）。タッチ端末では行の`draggable`をfalse化し、`cursor-grab`スタイルも外した
  - 上記だけでは長押し時の「沈む」見た目が解消せず、実機検証で再現。調査の結果、真因は`draggable`ではなく**モバイルブラウザ標準の「長押しでテキスト選択」機能**（行内のタイトル・メモ・日付等が素のテキストで`user-select`が既定の`auto`のままだったため）と判明。タッチ端末では行に`select-none`を追加し解消
  - 検証時、実機（スマホ）から同一Wi-Fi内のLAN経由でローカルdev serverにアクセスしようとしたところ、別の問題（Next.js 16のdev serverが`localhost`以外のオリジンからの`/_next/*`リソースアクセスをデフォルトでブロックする仕様）に遭遇。`next.config.ts`に`allowedDevOrigins: ["192.168.0.16"]`を追加して解消（今後もスマホ実機でLAN経由のdev server検証をする際に必要な設定）
  - `npm run lint` / `npm run build`: 問題なし。ユーザー本人が実機で長押し時に色が沈まなくなったことを確認済み「問題ないこと確認しました」

### タスク4: スマホ版アプリアイコンの修正
- 内容: `manifest.ts`のPWAアイコンが、`icon.png`をbase64で埋め込んだだけの「SVGの皮を被ったPNG」1枚（`purpose: "any"`のみ、maskable未対応）という代用実装になっており、Android実機の最近使ったアプリ（タスクスイッチャー）画面でアイコンが正しく表示されず、Androidが自動生成した水色の丸＋縮小されたアイコンになってしまう問題があった。正しいPWAアイコン構成（192/512のPNGアイコン＋maskable専用の512 PNG）に差し替える。maskable用の背景色はicon.png既存の背景に合わせて白を採用
- ステータス: 完了
- 実施内容:
  - `sharp`（node_modulesに既存）で`icon.png`から`icon-192.png`・`icon-512.png`（`purpose: "any"`）と、絵柄を中央65%の安全領域に白背景で配置した`icon-512-maskable.png`（`purpose: "maskable"`）を生成
  - `manifest.ts`の`icons`を、代用実装だった単一SVG（`icon.svg`のbase64埋め込みPNG）から上記3枚のPNG構成に差し替え。キャッシュ破棄用のバージョンクエリも`v=png_v1`に更新
  - 検証にあたり、スマホ実機からLAN経由（`http://192.168.0.16:3000`）でのアクセスでは、OAuthログイン後に本番URLへリダイレクトされる問題と、そもそもHTTP+生IPアドレスは「安全なコンテキスト」と見なされずPWAインストール自体が拒否される問題（Chromeの仕様、`localhost`のみ例外）に遭遇。`adb reverse tcp:3000 tcp:3000`でスマホの`localhost:3000`をPCのdev serverに直結する方法で解消し、ユーザー本人が実機でインストール・アイコン表示を確認済み「完璧です」
  - `npm run lint` / `npm run build`: 問題なし

---

## フェーズD: 一覧UIの改善

### タスク5: ページネーションの省略表示対応
- 内容: 一覧下部のページネーション（`Pagination.tsx`）は現状、総ページ数分の番号ボタンを`Array.from`で単純に全て並べているだけで、ページ数が増えると省略表示（例:「前へ 1 2 … 5 次へ」）にはならず、ボタンが折り返して増え続ける実装になっている。よくあるサイトのように、現在ページ周辺と先頭・末尾以外は「…」で省略する表示に改修する（ブラウザ・モバイル両対応）
- ステータス: 完了
- 実施内容:
  - `Pagination.tsx`に`getPageTokens()`を追加。現在ページの前後1ページ（`SIBLING_COUNT`）＋先頭＋末尾を表示し、間が空く場合は「…」を挟む方式に変更（例: `1 … 4 5 6 … 10`）。総ページ数が少ない場合（目安7ページ以下）は省略せず全番号を表示
  - 実データでは検証用の項目数が足りず複数ページが発生しなかったため、HANDOVER.md 4.6節の手法（一時的な認証不要ルート＋Playwright+Edge）で`Pagination`に様々な`currentPage`/`totalPages`の組み合わせを直接渡してスクリーンショットで確認。デスクトップ幅・モバイル幅（375px）の両方で省略表示とボタンの折り返しに問題がないことを確認後、一時ルート・スクリプトを削除し`npm install`で`node_modules`を復元
  - `npm run lint` / `npm run build`: 問題なし

---

## スコープ外（今回対応しない項目）

- ブラウザ拡張機能化
- MCP/API化
- 使い方ガイドボタンの設置（保留）: 将来的にはボタン押下でLP（使い方ガイド）ページへ遷移させる想定だが、そのLPサイト自体が未作成のため、ボタン設置自体を今回は見送る。LPサイトが用意できた段階で改めてタスク化する
- KVストレージ容量対策（保留）: 現状`contentText`（共有元AIの本文全文）は`user:{userId}:items:{itemId}`としてCloudflare KVにそのまま保存されており（`src/lib/kv/items.ts`）、indexには検索用に先頭300文字のスニペットのみを持つ設計（同ファイル21-24行目のコメント参照）。ユーザー数・本文サイズが増えた場合、KVの容量上限や従量コストが将来的にボトルネックになる可能性がある。対策候補として以下を検討したが、現時点では実測データ（1ユーザーあたりの平均`contentText`サイズ等）が無く着手判断できないため見送り。実測してから改めてタスク化する
  - 本文（`contentText`）をKVからR2へ切り出し、KVはメタデータ＋ポインタのみ保持する構成への変更（値サイズ上限25MiBの制約回避、GB単価の低減）
  - 保存前の圧縮（gzip/brotli等）
  - `user:{userId}:index`が全件を単一JSONで持つ現行設計の分割・ページネーション化（書き込み競合・肥大化の緩和。KV容量以前にこちらが先に詰まる可能性がある）
  - 古い/未アクセスのアイテムのアーカイブやTTLによる自動整理
  - ユーザーごとの容量クォータ導入と、超過時の有償プラン（ストレージ課金）への誘導

---

## 進捗ログ

（各タスク完了時にここへ日時・概要・テスト結果を追記する）
