# NEXT_PUBLIC_ 変数はブラウザ(クライアントコンポーネント)側のビルドに埋め込むため必要。
# .dev.vars と同じ値を、ここにも重複して設定してください。
# (.dev.vars はサーバー側/Cloudflare bindings用、.env.local はクライアント側ビルド用)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
