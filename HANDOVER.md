# ChatHub 今後の課題・次回開発要件（Ver0.1向け）

## 0. メタデータ取得に関する技術的知見・課題メモ
- ChatGPT, Claude, Gemini などの動的レンダリング/SPA共有リンクからのタイトル・本文取得は、Cloudflare Workers（エッジ環境）の単体スクレイピングでは限界がある。
- 将来的な抜本解決策として、「ローカル環境または専属サーバー（VPS等）上で Playwright を常駐させてセッションを維持し、それをプライベートAPI化してChatHubから叩く構造」の構築を検討・検討課題としてメモしておく。

## 1. タグ機能のバグ修正
- 新規作成・編集モーダル（画面）において、タグが追加できない問題を修正する。

## 2. タイポグラフィ・文字サイズ変更機能の調整
- 現在の「大」サイズを「中」として再定義し、「小・中・大」の3段階をスムーズに切替・スライドできるUIを実装する。

## 3. UI/デザイン・トグル・アイコンの調整
- 新規作成・編集時のトグルボタンを、新規作成ボタンと同じアクセントカラーの太線フレームで囲むデザインに変更する。
- 詳細フィルター内の「★」アイコン、および「お気に入りに登録」の表現を、テーブル一覧で使用されている黄色い★アイコンに統一し、「★お気に入り登録」表記に変更する。
- ヘッダーの設定アイコン（歯車）の隣に、初心者マークアイコン（🔰）の「使い方ガイド」ボタンを配置する。
- 検索フィルター周辺の領域の背景色を、全体の色彩に馴染む薄い補色（Subtle Complementary Color）に変更する。
- 詳細フィルター内の各項目の上下左右の間隔（Margin/Padding）が詰まりすぎているため、余白を広げて視認性を高める。

## 4. スクロール位置の復元（スクロールバック機能）
- 編集ボタンを押した際は「新規作成/編集領域」へ自動スクロールする現行動作を維持。
- 「保存」または「キャンセル」を押した際は、編集ボタンを最初に押した元のスクロール位置へ正確に画面を押し戻す（復元する）処理を実装する。

## 5. モバイル対応・サイドバー＆フォルダ操作の刷新
- スマホ画面表示時にフォルダ名が見づらいため、現在の表示位置をデフォルトとしつつ、サイドバーの幅・位置をユーザーが動かせる（リサイズ/スライド可能な）仕様にする。
- フォルダの並び替えUIを、現行の上下矢印（▲▼）ボタンからドラッグ＆ドロップ（Drag & Drop）による直感的な並び替えへ変更する。
- フォルダソート機能の選択肢を「任意の順序」「名前順↑」「名前順↓」の3種類のみにし、「作成日順」を削除する。

## 6. タグ・AIツールのカラーカスタマイズ機能
- タグごとにカラーを選択できるようにし、透明度が低め（薄く落ち着いたパステル調）のカラーパレットから選べるようにする。
- AIツールごとにカラーを選択できるようにし、透明度が高め（鮮やか・発色の良い）のカラーパレットから選べるようにする。
- ※両者の初期デフォルトカラーは適切に選定・設定すること。

## 7. フォルダ・一覧操作UIのシンプル化（漢字表記の排除）
- フォルダ一覧等の「改名」「削除」という漢字表記テキストをダサく見えないようアイコン化する：
  - 改名: モノトーン（白/グレー系統）のシンプルなペンアイコン（✏️風デザイン）
  - 削除: 小さめの赤色「×」アイコン
- 一覧テーブル内の「操作（編集/削除）」列にある削除ボタンも、同様に統一された小さな赤色「×」アイコンに変更する。

## 8. ブラウザ拡張機能への展望（将来検討・優先度未定）
- 現状はPWA（Androidの共有シート、iOSの手動貼り付けボタン）経由でのリンク登録が中心だが、Chrome/Edge等のブラウザ拡張機能を提供する構想を将来検討課題として残しておく。
- 拡張機能化することで見込める利点:
  - ChatGPT/Claude/Geminiの共有ページ（またはチャット画面そのもの）を開いている状態から、拡張機能のボタン一つでタイトル・本文・URLをその場で保存できるようになり、共有シート経由よりも手数が減る。
  - 0章で課題としているCloudflare Workers側のスクレイピング制約（SPA・動的レンダリング・Bot判定等）を、拡張機能側ではユーザー自身がログイン済みのブラウザ上で直接DOMから取得することで回避できる可能性がある。取得精度・安定性の面でBrowser Rendering/Jina Reader頼みの現行方式より根本的な改善が見込める。
  - 将来的にはVer0.1の各種UI改善（1〜7章）が一段落した後、0章のPlaywright常駐構想と合わせてどちらの方向性を取るか比較検討するのが望ましい。
  - ファビコン画像の角を丸めること
