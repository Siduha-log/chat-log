# ChatHub Ver0.1 開発 引き継ぎ資料

**このファイルは旧セッションから新セッションへの引き継ぎ用です。**
新しいセッションを開始したら、まずこのファイルを読み、次に **`plan_v0.1.md` を読んでください**。
`plan_v0.1.md` が唯一の正しいロードマップ兼進捗管理表（Single Source of Truth）です。
このファイル（handover.md）の内容と `plan_v0.1.md` の内容が食い違っていたら、**必ず `plan_v0.1.md` を優先**してください（本ファイルは執筆時点のスナップショットであり、その後 `plan_v0.1.md` だけが更新される可能性があるため）。

なお、旧 `handover.md`（このファイルの前バージョン。章立てで要求事項を列挙したもの）の内容は、
分析・タスク分解した上ですでに `plan_v0.1.md` に落とし込み済みです。旧handover.mdの章番号と
`plan_v0.1.md` のタスク番号は対応していません。**旧handover.mdの内容を元に再度タスク分解し直す
必要はありません。** `plan_v0.1.md` だけを見て進めてください。

---

## 1. 今どこまで進んでいるか

`plan_v0.1.md` のフェーズA〜D タスク5、および フェーズEのタスク8.5 が完了済みです（2026-09-18時点、全てコミット済み・sub-branchブランチ）。

**次にやるべきこと: フェーズD タスク6「フォルダ並び替えのドラッグ＆ドロップ化」から再開してください。**
その後 タスク7 → フェーズE残り（タスク8, 9）→ フェーズF（タスク10〜13）→ フェーズG（タスク14）の順です。

詳細な完了タスクの実施内容・学びは `plan_v0.1.md` の各タスクの「実施内容」欄と「進捗ログ」章に記録済みなので、作業前に必ず目を通してください。特に以下は再度つまずかないために重要です。

- タスク1（タグ追加バグ）: 表示バグとグローバル登録漏れの二重バグだった
- タスク2（文字サイズ）: UIはボタン式のまま維持（スライダー化は明確に拒否された）
- タスク3-1〜3-3（UI微調整）: 「トグルボタン」＝新規作成/編集フォーム自体を指す、と判明した経緯あり
- タスク3-2: 検索フィルターの背景色変更は最終的に見送り（元の背景色指定なしに戻した）。今後同種の要望が出ても、勝手に色を決め打ちせず具体的な値を先に確認するとよい
- タスク5: モバイルでの「ヘッダーのボタンが右端に寄りすぎる」報告は、実はパディング不足ではなくモバイル幅でのボタン群の横幅オーバーフローが真因だった

## 2. 開発ルール・進め方（ユーザーとの合意事項）

- **1タスクずつ**進める。次のタスクに進む前に必ずユーザーの確認を取る。
- 実装後は必ず `npm run lint` と `npm run build` を実行し、エラーがないことを確認してからユーザーに報告する。
- **ユーザーが明示的に「コミットしてください」と言うまでコミットしない。**
- ユーザーの確認が取れたら、`plan_v0.1.md` の該当タスクを「完了」に更新し、実施内容・学びを追記してから、コード変更と`plan_v0.1.md`を一緒にコミットする。
- コミットメッセージ・コード内コメント・会話は全て日本語（`CLAUDE.md`のプロジェクトルール）。
- `plan.md`（v0.0の記録）は絶対に編集しない。v0.1の計画・進捗は全て `plan_v0.1.md` に書く。
- UIの見た目・色・間隔などの微調整系の要望は、言葉だけでは正確に伝わらないことが多い。可能なら値を確認しながら小刻みに調整するとよい（タスク3-2の背景色調整で何度か手戻りが発生した実例あり）。

## 3. 環境まわりの重要な注意点（つまずきポイント）

### 3.1 このマシンのセットアップ状況
- Node.js LTS は winget 経由でインストール済み（`C:\Program Files\nodejs`）。
- PowerShellの実行ポリシーは `RemoteSigned`（CurrentUserスコープ）に変更済み。これがないと `npm` コマンドがブロックされる。
- `.env.local` / `.dev.vars` には**実際のSupabase認証情報**が設定済み（プレースホルダーではない）。これらは `.gitignore` 対象。

### 3.2 Supabaseのリダイレクト設定（重要・過去にハマった）
Supabaseダッシュボードの Authentication > URL Configuration > Redirect URLs に `http://localhost:3000/**` が登録済み。**もしOAuthログイン後に本番URL（`chathub-app.dev2logging.workers.dev`）へ飛ばされる不具合が再発したら、まずここを疑うこと。**

### 3.3 「直したのに直ってない」と言われたら
過去に、ユーザーが実際には本番デプロイ済みURL（`chathub-app.dev2logging.workers.dev`）を見ていて、localhost:3000への変更が反映されていないように見えた、という事故があった。修正が反映されないという報告を受けたら、**まずブラウザのアドレスバーの表示URLを確認してもらうこと。**

### 3.4 npm install は dev server 停止中にのみ実行する（重要・事故発生済み）
検証用に `playwright-core` を一時インストール/削除する際、**ユーザーのdev serverが稼働中のまま `npm install` を実行すると、Windowsのファイルロック（`workerd.exe`使用中）でdev serverプロセスが落ちる。** 実際に一度事故を起こしている。`npm install` を実行する前に `Get-NetTCPConnection -LocalPort 3000 -State Listen` 等でdev serverの状態を確認するか、ユーザーに一言断ってから実行すること。

### 3.5 UI検証の方法（認証なしで確認する手法）
このアプリはSupabase認証必須のため、Claude自身がPlaywright等で本物のログインフローを通しにくい（メール確認が必要、実アカウントを増やしたくない等）。代わりに以下の手法を確立した:

1. 検証したいコンポーネント/JSX構造を `src/app/dev-xxxtest/page.tsx` のような**一時的な認証不要ルート**として作成し、ダミーpropsで直接レンダリングする
2. `npm install --no-save --no-package-lock playwright-core` で一時インストール（**dev server停止中に実行、または3.4節に注意**）
3. Edge実体（`C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`）を `chromium.launch({ executablePath: ... })` で起動して操作・スクリーンショット確認
4. 確認できたら、一時ルート・スクリプト・スクリーンショットを全て削除し、`npm install`（引数なし）で `node_modules` をロックファイルの状態に戻す

### 3.6 その他
- `git status` / `git diff` は毎回ビルド後に `.next` ディレクトリを `rm -rf .next` で削除してから確認すると、生成物がノイズにならない。
- ユーザーはWindows環境。Bashツール（Git Bash）とPowerShellツールを併用しているが、PATHが同期していないことがあるため、`npm`系コマンドはPowerShellツールで、ファイル操作はBashツールで、と使い分けている。PowerShellで`npm`を使う際は毎回 `$env:Path` にmachine+userのPATHを読み込み直す必要がある場面があった（新規プロセスなら通常不要）。

---

以上を踏まえて、`plan_v0.1.md` を開き、タスク6から作業を再開してください。
