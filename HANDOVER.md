# ChatHub 開発 引き継ぎ資料

**このファイルは旧セッションから新セッションへの引き継ぎ用です。**
新しいセッションを開始したら、まずこのファイルを読み、次に **`plan_v0.3.md` を読んでください**。
`plan_v0.3.md` が唯一の正しいロードマップ兼進捗管理表（Single Source of Truth）です。
このファイル（HANDOVER.md）の内容と `plan_v0.3.md` の内容が食い違っていたら、**必ず `plan_v0.3.md` を優先**してください（本ファイルは執筆時点のスナップショットであり、その後 `plan_v0.3.md` だけが更新される可能性があるため）。

なお `plan.md`（v0.0の記録）・`plan_v0.1.md`（v0.1の記録。フェーズA〜F完了）・`plan_v0.2.md`（v0.2の記録。タスク1除き完了）はいずれも過去の記録であり、今後編集しない。v0.3以降の計画・進捗は全て `plan_v0.3.md` に書く。

---

## 1. 今どこまで進んでいるか

**v0.2（タスク2〜5）が全て完了し、本番環境へのデプロイも完了済みです**（2026-09-18時点）。

- タスク2: パスキーのリネーム機能（設定画面）
- タスク3: モバイルで一覧行を長押しした時の意味のない選択状態の解消
- タスク4: PWAアプリアイコンの修正（maskable対応）
- タスク5: ページネーションの省略表示対応

**タスク1「OGP/メタタグからのタイトル取得可否の調査」は保留にしました。** 調査の結果、
タイトルのOGP取得は実は v0.0 の時点で既に実装済みであることが判明し、単発タスクとして
再着手するのではなく、独立した実装計画として `plan_v0.3.md` に切り出しています。
**次のセッションは `plan_v0.3.md` を読み、そこに書かれた「3. 再定義した調査ゴール」から
着手してください。** 特に3-1（本文取得の非同期化）に取り組む価値があるか、着手前に
必ずユーザーに確認すること。

作業ブランチ `sub-branch-task14` は `master` にマージ・push済み、`npm run deploy` で
本番環境（https://chathub-app.dev2logging.workers.dev ）への反映も完了しています。

v0.1・v0.2完了タスクの実施内容・学びはそれぞれ `plan_v0.1.md`・`plan_v0.2.md`（凍結済み・
参照専用）の各タスクの「実施内容」欄に記録済みなので、作業前に必ず目を通してください。
特に以下は再度つまずかないために重要です（新しいものほど重要度が高いので上に記載）。

- **タスク4（PWAアイコン）関連（新規・重要）**: スマホ実機でPWAの見た目やインストールを検証する際、`http://<LAN IP>:3000`（例: `192.168.0.16:3000`）でのアクセスには2つの罠がある。(1) Next.js 16のdev serverは`localhost`以外のオリジンからの`/_next/*`アクセスをデフォルトでブロックするため`next.config.ts`に`allowedDevOrigins`の追加が必要、(2) それでアクセスできても、生IPアドレス+HTTPは「安全なコンテキスト」と見なされずPWAのインストール自体が拒否される（Chromeの仕様、`localhost`のみ例外）。**確実なのはUSB経由で`adb reverse tcp:3000 tcp:3000`を使い、スマホから`http://localhost:3000`として繋ぐ方法**（4.9節に手順）。また、この経路だとSupabaseの「許可されたリダイレクトURL」もlocalhost向けの既存設定がそのまま使えるため、OAuthログイン時に本番URLへ飛ばされる問題も同時に解消する
- **タスク3（モバイル長押し）**: 一覧行が「沈んで見える」原因はHTML5の`draggable`属性ではなく、**モバイルブラウザ標準の「長押しでテキスト選択」機能**（`user-select`が既定`auto`のまま）だった。`select-none`で解消。同種の「長押しで何か変な見た目になる」系の不具合が出たら、まずdraggable関連ではなくテキスト選択を疑うとよい
- **タスク2（パスキー）**: `friendly_name`は端末のパスキーマネージャーが自己申告する値（例: 複数端末で全部「Google Password Manager」になり区別できない）。`supabase.auth.passkey.update({ passkeyId, friendlyName })`でリネーム可能
- **タスク12（マジックリンク）**: シークレットタブでマジックリンクをリクエストし、別のタブ（通常タブ）でリンクを開くとログインに失敗する。PKCEフローの検証用の鍵がブラウザのlocalStorageに保存される仕様上の**意図された挙動（バグではない）**
- **タスク10/11（パスキー登録）**: Supabaseの`registerPasskey()`は「既にログイン済みの、確認済みユーザー」でないと呼び出せない制約がある。**登録は設定画面（`SettingsDialog.tsx`）、ログインはログイン画面**、と機能を分けて実装した
- **タスク8/9（カラーカスタマイズ）+ UI微調整全般**: 新しいUI機能は実装しただけでは終わらず、実際に触ったユーザーから連鎖的なフィードバックが来ることが多い。一区切りついたと思っても、コミット前に一往復置くと後戻りが減る
- **タスク6（フォルダD&D）**: HTML5ネイティブドラッグ＆ドロップAPIはモバイルブラウザのタッチ操作では発火しない。モバイル対応が必要な画面では最初からPointer Events方式を使うこと

## 2. 現状のアプリ仕様まとめ（引き継ぎ時点）

### 2.1 認証方式
- **OAuth**（Google/GitHub/X）: 誰でも使える主要な入口。ログイン画面の初期表示はこれのみ
- **パスキー（WebAuthn、experimental）**: 「その他のログイン方法（2回目以降の方はこちら）」を開くと表示。登録は`SettingsDialog.tsx`から、既にログイン済みの状態でのみ可能。設定画面から任意の名前にリネーム可能（タスク2）
- **マジックリンク**: パスキーがさらに使えない場合の最終手段（ネストしたトグルの一番奥）
- メールアドレス/パスワード認証は完全廃止済み

### 2.2 タグ・AIツールのカラーカスタマイズ
- タグ: パステル調プリセットパレット10色（`src/lib/tagColors.ts`）、AIツール: 鮮やか系10色（`src/lib/aiToolColors.ts`）
- データには色のhex値ではなくパレットのidのみ保存。色の変更操作は管理画面（`TagPanel.tsx`/`AiToolPanel.tsx`）のみに一元化

### 2.3 文字数制限
| 項目 | 上限 |
|---|---|
| タグ名 / AIツール名 | 12文字 |
| フォルダ名 | 15文字 |
| タイトル | 30文字 |
| メモ | 140文字 |
| パスキー名（タスク2で新設） | 20文字（`PASSKEY_NAME_MAX_LENGTH`） |
| 本文（contentText） | 上限なし |

すべて `src/lib/constants.ts` に定義。

### 2.4 一覧・モバイルUI
- 一覧行はモバイル（タッチ操作、`pointer: coarse`判定）では`draggable`と`select-none`で長押し時の意味のない選択状態を無効化。PC（マウス）ではドラッグでアイテム同士を新フォルダにまとめる機能は維持（`ItemTable.tsx`）
- ページネーションは`Pagination.tsx`で「1 … 4 5 6 … 10」のような省略表示に対応済み（`SIBLING_COUNT`で前後表示数を調整可能）
- PWAアイコンは`icon-192.png`/`icon-512.png`（any）＋`icon-512-maskable.png`（maskable、白背景に65%サイズで中央配置）の3枚構成（`manifest.ts`）。アイコン画像を差し替える際は`?v=`のクエリ値を必ず変えること（実機のインストールキャッシュ対策）

## 3. 開発ルール・進め方（ユーザーとの合意事項）

- **1タスクずつ**進める。次のタスクに進む前に必ずユーザーの確認を取る。
- 実装後は必ず `npm run lint` と `npm run build` を実行し、エラーがないことを確認してからユーザーに報告する。
- **タスクが完了しユーザーの確認が取れたら、都度コミットする**（2026-09-18のセッションで「タスク終了ごとにコメントしてコミットして」と明示的に合意済み。それ以前は「明示的に指示されるまでコミットしない」方針だったが、今後は各タスク完了時に自動的にコミットする運用に変更）。
- コミット前に、該当する`plan_v0.3.md`のタスクを「完了」に更新し、実施内容・テスト結果を追記してから、コード変更と一緒にコミットする。
- コミットメッセージ・コード内コメント・会話は全て日本語（`CLAUDE.md`のプロジェクトルール）。
- `plan.md`・`plan_v0.1.md`・`plan_v0.2.md`は絶対に編集しない。v0.3の計画・進捗は全て `plan_v0.3.md` に書く。
- UIの見た目・色・間隔などの微調整系の要望は、言葉だけでは正確に伝わらないことが多い。可能なら値を確認しながら小刻みに調整するとよい。
- **実機で動作確認が必要な機能**（ドラッグ＆ドロップ、認証まわり、メール送信を伴う機能、PWAインストール等）は、エージェント自身では検証できないため、実装後は必ずユーザーに実機確認を依頼し、その結果を待ってから完了扱いにすること。
- 認識のズレが起きやすい抽象的な指摘を受けたときは、可能であればスクリーンショットを求め、こちらの理解を一度言語化してユーザーに確認してから編集に入ると手戻りが減る。
- 実データが少なく画面上で確認しづらいUI変更（例: ページネーションの省略表示）は、一時的な認証不要ルート＋Playwrightのスクリーンショットで自己検証してから報告すると手戻りが減る（4.6節）。

## 4. 環境まわりの重要な注意点（つまずきポイント）

### 4.1 このマシンのセットアップ状況
- Node.js LTS は winget 経由でインストール済み（`C:\Program Files\nodejs`）。
- PowerShellの実行ポリシーは `RemoteSigned`（CurrentUserスコープ）に変更済み。
- `.env.local` / `.dev.vars` には**実際のSupabase認証情報**が設定済み（`.gitignore`対象）。
- Supabaseダッシュボードのパスキー設定（Relying Party関連）は**本番ドメイン向け**に設定済み（ローカル`localhost`向けの設定は上書きされている）。
- `wrangler`はこのマシン上でCloudflareアカウント（`dev2logging@gmail.com`）にOAuthログイン済み。`npx wrangler whoami`で未認証と出た場合のみ`npx wrangler login`が必要。
- **Android Platform Tools（adb）をwingetでインストール済み**（`winget install Google.PlatformTools`）。4.9節参照。

### 4.2 Supabaseのリダイレクト設定（重要・過去にハマった）
Supabaseダッシュボードの Authentication > URL Configuration > Redirect URLs に、現時点で以下4件が登録済み。
```
http://localhost:3000/auth/callback
https://chathub-app.dev2logging.workers.dev/auth/callback
http://localhost:3000/**
http://192.168.0.16:3000/**
```
**LAN IP（`http://192.168.0.16:3000/**`）を登録してもOAuthログイン後に本番URLへ飛ばされる問題が解消しなかった実例がある**（原因未特定。IPアドレス形式のホストに対するSupabase側のワイルドカードマッチングの制約の可能性）。LAN IP経由でのログインがどうしても必要な場合は、設定を疑うより先に4.9節の`adb reverse`で`localhost`として扱わせる方が確実で早い。

### 4.3 マジックリンクはシークレットタブと通常タブをまたぐと失敗する
PKCEフローの検証用の鍵はブラウザのlocalStorageに保存されるため、**マジックリンクをリクエストしたのと別のブラウザ/タブでリンクを開くと認証に失敗する**。仕様上の制約でありバグではない。

### 4.4 「直したのに直ってない」と言われたら
ユーザーが実際には本番デプロイ済みURLを見ていて、localhost:3000への変更が反映されていないように見える、という事故が過去にあった。**まずブラウザのアドレスバーの表示URLを確認してもらうこと。**

### 4.5 npm install は dev server 停止中にのみ実行する（重要・事故発生済み）
**ユーザーのdev serverが稼働中のまま `npm install` を実行すると、Windowsのファイルロック（`workerd.exe`使用中）でdev serverプロセスが落ちる。** 実行前に`Get-NetTCPConnection -LocalPort 3000 -State Listen`等で状態を確認するか、一言断ってから実行すること。

### 4.6 UI検証の方法（認証なしで確認する手法）
1. 検証したいコンポーネント/JSX構造を `src/app/dev-xxxtest/page.tsx` のような**一時的な認証不要ルート**として作成し、ダミーpropsで直接レンダリングする
2. `npm install --no-save --no-package-lock playwright-core` で一時インストール（**dev server停止中に実行**、4.5節参照）
3. Edge実体（`C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`）を `chromium.launch({ executablePath: ... })` で起動して操作・スクリーンショット確認（デスクトップ幅・モバイル幅375px両方を撮ると良い）
4. 確認できたら、一時ルート・スクリプト・スクリーンショットを全て削除し、`npm install`（引数なし）で `node_modules` をロックファイルの状態に戻す

認証を伴う機能や実機のタッチ操作等はこの方法でも検証できないため、**ユーザー本人による実機確認が必須**（3章参照）。

### 4.7 本番反映の手順
1. 作業ブランチから`master`へのマージ（PR経由 or ローカルで直接マージ）
2. コンフリクトが出た場合はユーザーと一緒に解消方針を決める
3. ローカルの`master`を最新化（未コミットの変更があると`checkout`できないので`git stash`で退避してから）
4. `npx wrangler whoami`で認証状態を確認し、未認証なら`npx wrangler login`
5. `npm run deploy`（内部で`opennextjs-cloudflare build && opennextjs-cloudflare deploy`を実行）。出力末尾の本番URLとVersion IDを確認
6. ユーザーに本番URLでの実機確認を依頼

### 4.8 その他
- `git status` / `git diff` は毎回ビルド後に `.next` ディレクトリを `rm -rf .next` で削除してから確認するとノイズが減る。**ただし`npm run dev`が稼働中の場合は`.next`を削除するとdev serverが「ディレクトリが削除された」と検知して自動再起動する**（実害はないが、build直後に毎回消すのではなく、dev server停止中にまとめて掃除する方が安全）。
- ユーザーはWindows環境。Bashツール（Git Bash）とPowerShellツールを併用しており、`npm`系コマンドはPowerShellツールで、ファイル操作はBashツールで、と使い分けている。PowerShellで`npm`を使う際は毎回 `$env:Path` にmachine+userのPATHを読み込み直す必要がある場面があった。
- バックグラウンドで起動した`npm run dev`を`TaskStop`等で停止しても、**Windows側でnode.exeの実プロセスが残ってポート3000を掴んだままになることがある**。`Get-NetTCPConnection -LocalPort 3000 -State Listen`で確認し、残っていれば`taskkill //PID <PID> //F`（Bashツールでは`//`、PowerShellでは`/`）で individually終了させてから再起動すること。

### 4.9 スマホ実機でLAN経由のdev serverを確実に検証する方法（新規・重要）
`http://<LAN IP>:3000`（例: `192.168.0.16:3000`）経由のスマホ実機検証には、次の問題が積み重なることが判明した。
1. Next.js 16 dev serverの`allowedDevOrigins`未設定によるブロック → `next.config.ts`に`allowedDevOrigins: ["<LAN IP>"]`を追加すれば解消（済み）
2. 生IPアドレス+HTTPは「安全なコンテキスト」と見なされず、PWAインストールが拒否される（Chrome仕様、`localhost`のみ例外）
3. OAuthログイン後、Supabaseの許可リダイレクトURLにLAN IPを追加しても本番URLへ飛ばされる現象が解消しないことがあった（原因未特定）

これら全てを回避する確実な方法が**`adb reverse`によるUSB接続**。手順:
1. スマホ側: 「設定」→「デバイス情報」→「ビルド番号」を7回タップして開発者向けオプションを有効化 → 「開発者向けオプション」→「USBデバッグ」をON
2. PCとスマホをUSBケーブルで接続し、スマホ側で「USBデバッグを許可しますか？」を許可（「常に許可」推奨）
3. PC側: `winget install Google.PlatformTools`でadbを導入済み（未導入なら実行）
4. `adb devices`でスマホが`device`と表示されることを確認（`unauthorized`の場合はスマホ側の許可ダイアログを確認）
5. `adb reverse tcp:3000 tcp:3000`を実行
6. スマホのChromeで`http://localhost:3000`にアクセス（`localhost`指定なのでSupabaseのリダイレクトURL設定もPWAインストール要件もそのまま満たされる）

**USBケーブルを抜くと`adb reverse`の接続も切れる**ため、再度使う場合は3〜5を再実行すること（adb自体の再インストールは不要）。

---

以上を踏まえて、`plan_v0.3.md`の「4. 次回着手時の進め方」に沿って作業を再開してください。
