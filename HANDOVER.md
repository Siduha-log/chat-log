# ChatHub Ver0.1 開発 引き継ぎ資料

**このファイルは旧セッションから新セッションへの引き継ぎ用です。**
新しいセッションを開始したら、まずこのファイルを読み、次に **`plan_v0.2.md` を読んでください**。
`plan_v0.2.md` が唯一の正しいロードマップ兼進捗管理表（Single Source of Truth）です。
このファイル（handover.md）の内容と `plan_v0.2.md` の内容が食い違っていたら、**必ず `plan_v0.2.md` を優先**してください（本ファイルは執筆時点のスナップショットであり、その後 `plan_v0.2.md` だけが更新される可能性があるため）。

なお `plan.md`（v0.0の記録）・`plan_v0.1.md`（v0.1の記録。フェーズA〜F完了、2026-09-18時点で凍結済み）はどちらも過去の記録であり、今後編集しない。v0.2以降の計画・進捗は全て `plan_v0.2.md` に書く。

---

## 1. 今どこまで進んでいるか

**v0.1（フェーズA〜F）が全て完了しました**（2026-09-18時点）。`sub-branch`で実装 → PR #1を作成 → **`master`にマージ済み** → **`npm run deploy`で本番環境（https://chathub-app.dev2logging.workers.dev ）へのデプロイも完了済み**です。ユーザー本人が本番URLで動作確認し「問題ないことを確認しました」とのこと。

**v0.1は`plan_v0.1.md`ごと凍結し、ここからは新規に作成した`plan_v0.2.md`に沿って開発を進めます。** 旧フェーズG タスク14「OGP/メタタグからのタイトル取得可否の調査」は、`plan_v0.2.md` の フェーズA タスク1として引き継ぎ済みです（内容は変更なし）。

**セットアップ・環境確認は完了済みです**（同セッション内で実施）:
- `master`ブランチから作業ブランチ `sub-branch-task14` を作成済み
- `npm install`実施済み（`node_modules`再構築）、`npm run lint` / `npm run build` とも問題なし
- `npx wrangler whoami`で認証状態（`dev2logging@gmail.com`）確認済み

**次にやるべきこと:**
1. （ブランチ作成済みのため）`plan_v0.2.md` のタスク1に着手する
2. 従来通り、`npm run dev`でローカルホスト（localhost:3000）上で動作確認しながら実装を進める
3. タスク完了・ユーザー確認・コミットまで済んだら、今回と同様に**PR作成 → masterへのマージ → `npm run deploy`で本番反映**という流れになる見込み（本番反映のタイミングはユーザーの指示を待つこと。都度確認する）

内容: 「Notionと同様に、タイトルだけであれば OGP/メタタグ情報（`<title>`, `og:title` 等）から取得できるのではないか」という仮説を検証する調査タスクです。ChatGPT/Claude/Gemini の共有ページで実際に取得可能か調査し、現行のスクレイピング処理（Cloudflare Browser Rendering / Jina Reader、`src/app/api/metadata/route.ts`）との比較検討を行ってください。調査結果次第では、軽量なOGPタグ取得だけで済むページとフル本文スクレイピングが必要なページを判別する分岐実装が発生する可能性があります（実装するかどうかも含めてユーザーと相談してください）。

v0.1完了タスクの実施内容・学びは `plan_v0.1.md`（凍結済み・参照専用）の各タスクの「実施内容」欄に記録済みなので、作業前に必ず目を通してください。特に以下は再度つまずかないために重要です（新しいものほど重要度が高いので上に記載）。

- **タスク12（マジックリンク）**: シークレットタブでマジックリンクをリクエストし、別のタブ（通常タブ）でリンクを開くとログインに失敗する。これはPKCEフローの検証用の鍵がブラウザのlocalStorageに保存される仕様上、タブ間で保存領域が分離されていると起きる**意図された挙動（バグではない）**。同じブラウザで普通にメールを確認する分には問題ない。テスト時にこれで「バグでは」と混乱しないこと
- **タスク10/11（パスキー）**: Supabaseの`registerPasskey()`は「既にログイン済みの、確認済みユーザー」でないと呼び出せない制約がある。パスキーだけでは新規アカウントを作れないため、**登録は設定画面（`SettingsDialog.tsx`）、ログインはログイン画面**、と機能を分けて実装した
- **タスク8/9（カラーカスタマイズ）+ UI微調整全般**: 新しいUI機能は実装しただけでは終わらず、実際に触ったユーザーから「ホバーでちらつく」「モバイルで動かない」「配置がダサい」等、連鎖的なフィードバックが来ることが多かった。一区切りついたと思っても、コミット前に一往復置くと後戻りが減る
- **ホバー時のちらつきバグ（タスク9関連）**: 一覧の折り返しレイアウト（`flex flex-wrap`）内の要素をホバーで横に広げると、他要素の折り返し位置がズレてホバーON/OFFが連鎖する「ちらつき」が発生する。解決策は、ホバーで表示する操作UIを**一覧の折り返しレイアウトからは完全に分離した専用エリア**に置くこと（`TagPanel.tsx`/`AiToolPanel.tsx`参照）
- **タスク6（フォルダD&D）**: HTML5ネイティブドラッグ＆ドロップAPIはモバイルブラウザのタッチ操作では発火しない。モバイル対応が必要な画面では最初からPointer Events（`pointerdown`/`pointermove`/`pointerup`）方式を使うこと
- **タスク1（タグ追加バグ）**: 表示バグとグローバル登録漏れの二重バグだった
- **タスク2（文字サイズ）**: UIはボタン式のまま維持（スライダー化は明確に拒否された）
- **タスク3-2**: 検索フィルターの背景色変更は最終的に見送り。色味の要望は具体的な値を先に確認するとよい
- **タスク5**: モバイルでの「ヘッダーのボタンが右端に寄りすぎる」報告は、パディング不足ではなくモバイル幅でのボタン群の横幅オーバーフローが真因だった

## 2. 現状のアプリ仕様まとめ（引き継ぎ時点）

### 2.1 認証方式（フェーズFで刷新済み）
- **OAuth**（Google/GitHub/X）: 誰でも使える主要な入口。ログイン画面の初期表示はこれのみ
- **パスキー（WebAuthn、experimental）**: 「その他のログイン方法（2回目以降の方はこちら）」を開くと表示。登録は`SettingsDialog.tsx`から、既にログイン済みの状態でのみ可能
- **マジックリンク**: パスキーがさらに使えない場合の最終手段（ネストしたトグルの一番奥）。`signInWithOtp()`で送信し、`/auth/callback`のPKCEコード交換（`exchangeCodeForSession`）で受ける
- **メールアドレス/パスワード認証は完全廃止済み**（`signInWithPassword`/`signUp`の呼び出し箇所は無し）
- Supabaseダッシュボード側のパスキー設定（Relying Party Display Name/ID/Origins）は**ユーザー本人が設定済み**

### 2.2 タグ・AIツールのカラーカスタマイズ（フェーズE）
- タグ: パステル調プリセットパレット10色（`src/lib/tagColors.ts`）
- AIツール: 鮮やか・発色の良いプリセットパレット10色（`src/lib/aiToolColors.ts`）
- どちらもデータには色のhex値ではなくパレットのidのみ保存し、見た目はコード側で一元管理（後から色味を調整してもデータ移行が不要な設計）
- 色の変更操作は**管理画面（`TagPanel.tsx`/`AiToolPanel.tsx`）のみ**に一元化（新規登録/編集フォームでは表示のみで変更不可、という仕様）

### 2.3 文字数制限（タスク9関連の追加修正で確定）
| 項目 | 上限 |
|---|---|
| タグ名 | 12文字（`TAG_NAME_MAX_LENGTH`） |
| AIツール名 | 12文字（`AI_TOOL_NAME_MAX_LENGTH`） |
| フォルダ名 | 15文字（`FOLDER_NAME_MAX_LENGTH`） |
| タイトル | 30文字（`TITLE_MAX_LENGTH`） |
| メモ | 140文字（`MEMO_MAX_LENGTH`） |
| 本文（contentText） | **上限なし**（`CONTENT_MAX_LENGTH`は撤廃済み） |

すべて `src/lib/constants.ts` に定義。タグ・AIツール・フォルダ名は超過時にAPIがエラーを返す方式、タイトル・メモはサーバー側で超過分を静かに切り詰める方式（意図的な使い分け）。

### 2.4 その他UI仕様
- モバイル・ブラウザ両対応のフォルダ並び替えD&D（Pointer Events方式）
- ヘッダーは`sticky top-0`で画面幅いっぱいに固定表示（`page.tsx`の`<header>`は`max-w-[1600px]`コンテナの外側、`<body>`直下の兄弟要素として配置）
- テーブルの並び替えは、詳細フィルターではなくテーブル見出し列（タイトル/メモ・日付）の⇅アイコンから行う（ブラウザ版）。モバイル版はテーブル上部の専用セレクトから

## 3. 開発ルール・進め方（ユーザーとの合意事項）

- **1タスクずつ**進める。次のタスクに進む前に必ずユーザーの確認を取る。
- 実装後は必ず `npm run lint` と `npm run build` を実行し、エラーがないことを確認してからユーザーに報告する。
- **ユーザーが明示的に「コミットしてください」と言うまでコミットしない。**
- ユーザーの確認が取れたら、`plan_v0.2.md` の該当タスクを「完了」に更新し、実施内容・学びを追記してから、コード変更と`plan_v0.2.md`を一緒にコミットする。
- コミットメッセージ・コード内コメント・会話は全て日本語（`CLAUDE.md`のプロジェクトルール）。
- `plan.md`（v0.0の記録）・`plan_v0.1.md`（v0.1の記録）は絶対に編集しない。v0.2の計画・進捗は全て `plan_v0.2.md` に書く。
- UIの見た目・色・間隔などの微調整系の要望は、言葉だけでは正確に伝わらないことが多い。可能なら値を確認しながら小刻みに調整するとよい。
- **実機で動作確認が必要な機能**（ドラッグ＆ドロップ、認証まわり、メール送信を伴う機能等）は、エージェント自身では検証できないため、実装後は必ずユーザーに実機確認を依頼し、その結果を待ってから完了扱いにすること。
- 認識のズレが起きやすい抽象的な指摘（「色が塗られていない」「なんかダサい」等）を受けたときは、可能であればスクリーンショットを求め、こちらの理解を一度言語化してユーザーに確認してから編集に入ると手戻りが減る。

## 4. 環境まわりの重要な注意点（つまずきポイント）

### 4.1 このマシンのセットアップ状況
- Node.js LTS は winget 経由でインストール済み（`C:\Program Files\nodejs`）。
- PowerShellの実行ポリシーは `RemoteSigned`（CurrentUserスコープ）に変更済み。これがないと `npm` コマンドがブロックされる。
- `.env.local` / `.dev.vars` には**実際のSupabase認証情報**が設定済み（プレースホルダーではない）。これらは `.gitignore` 対象。
- Supabaseダッシュボードのパスキー設定（Relying Party関連）は**本番ドメイン（`chathub-app.dev2logging.workers.dev`）向けに設定変更済み**（ローカル`localhost`向けの設定は上書きされたため、ローカルでのパスキーテストは現状できない可能性がある。Relying Party IDはSupabase側で1つしか設定できない仕様のため）。
- `wrangler`はこのマシン上でCloudflareアカウント（`dev2logging@gmail.com`）にOAuthログイン済み（認証情報は`C:\Users\bambo\AppData\Roaming\xdg.config\.wrangler\config\default.toml`に保存）。次回セッションで改めて`wrangler login`する必要は基本的にないはずだが、`npx wrangler whoami`で未認証と出た場合は再ログインが必要（ブラウザでのOAuth許可が必要。過去に「Your account uses a different login provider」というCloudflare側のログインエラーが出たことがあり、その場合はGoogle/GitHub等の別のログインボタンを探すよう案内すること）。

### 4.2 Supabaseのリダイレクト設定（重要・過去にハマった）
Supabaseダッシュボードの Authentication > URL Configuration > Redirect URLs に `http://localhost:3000/**` が登録済み。**もしOAuth/マジックリンクログイン後に本番URL（`chathub-app.dev2logging.workers.dev`）へ飛ばされる不具合が再発したら、まずここを疑うこと。**

### 4.3 マジックリンクはシークレットタブと通常タブをまたぐと失敗する（新規・重要）
PKCEフローの検証用の鍵はブラウザのlocalStorageに保存されるため、**マジックリンクをリクエストしたのと別のブラウザ/タブ（特にシークレットタブと通常タブの組み合わせ）でリンクを開くと認証に失敗する**。これは仕様上の制約でありバグではない。動作確認時は同じブラウザ・同じプロファイル内で完結させること。失敗時の理由は`console.error`に出力される（`/auth/callback` → `login/page.tsx`の`callbackError`初期化ロジック参照）。

### 4.4 「直したのに直ってない」と言われたら
過去に、ユーザーが実際には本番デプロイ済みURL（`chathub-app.dev2logging.workers.dev`）を見ていて、localhost:3000への変更が反映されていないように見えた、という事故があった。修正が反映されないという報告を受けたら、**まずブラウザのアドレスバーの表示URLを確認してもらうこと。**

### 4.5 npm install は dev server 停止中にのみ実行する（重要・事故発生済み）
検証用に `playwright-core` を一時インストール/削除する際、**ユーザーのdev serverが稼働中のまま `npm install` を実行すると、Windowsのファイルロック（`workerd.exe`使用中）でdev serverプロセスが落ちる。** 実際に一度事故を起こしている。`npm install` を実行する前に `Get-NetTCPConnection -LocalPort 3000 -State Listen` 等でdev serverの状態を確認するか、ユーザーに一言断ってから実行すること。

### 4.6 UI検証の方法（認証なしで確認する手法）
このアプリはSupabase認証必須のため、Claude自身がPlaywright等で本物のログインフローを通しにくい（メール確認が必要、実アカウントを増やしたくない等）。代わりに以下の手法を確立した:

1. 検証したいコンポーネント/JSX構造を `src/app/dev-xxxtest/page.tsx` のような**一時的な認証不要ルート**として作成し、ダミーpropsで直接レンダリングする
2. `npm install --no-save --no-package-lock playwright-core` で一時インストール（**dev server停止中に実行、または4.5節に注意**）
3. Edge実体（`C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`）を `chromium.launch({ executablePath: ... })` で起動して操作・スクリーンショット確認
4. 確認できたら、一時ルート・スクリプト・スクリーンショットを全て削除し、`npm install`（引数なし）で `node_modules` をロックファイルの状態に戻す

なお、認証を伴う機能（パスキー登録、マジックリンク送信・受信、ドラッグ＆ドロップの実機タッチ操作等）は上記の方法でも検証できないため、**ユーザー本人による実機確認が必須**（3章参照）。

### 4.7 本番反映の手順（今回のセッションで確立）
ユーザーから「本番に反映して」と依頼された場合の流れ:
1. 作業ブランチ（`sub-branch`等）からmasterへのPRを作成（`gh pr create --base master --head <作業ブランチ> ...`）
2. GitHub上でコンフリクトが出た場合はユーザーと一緒に解消方針を決めてから、GitHub上またはローカルで解消
3. PRマージ後、ローカルの`master`を`git checkout master && git pull origin master`で最新化
   - このとき**ローカルに未コミットの変更（特にHANDOVER.mdの下書き等）があるとcheckoutできない**ので、`git stash push -- <ファイル>`で退避してから切り替え、切り替え後に`git stash pop`で戻すこと
4. `npx wrangler whoami`で認証状態を確認し、未認証なら`npx wrangler login`（バックグラウンド実行推奨。ブラウザでの許可待ちになるため、ユーザーに完了を確認してもらってから次に進む）
5. `npm run deploy`（内部で`opennextjs-cloudflare build && opennextjs-cloudflare deploy`を実行）。出力末尾に表示される本番URLとVersion IDを確認
6. ユーザーに本番URLでの実機確認を依頼

### 4.8 その他
- `git status` / `git diff` は毎回ビルド後に `.next` ディレクトリを `rm -rf .next` で削除してから確認すると、生成物がノイズにならない。
- ユーザーはWindows環境。Bashツール（Git Bash）とPowerShellツールを併用しているが、PATHが同期していないことがあるため、`npm`系コマンドはPowerShellツールで、ファイル操作はBashツールで、と使い分けている。PowerShellで`npm`を使う際は毎回 `$env:Path` にmachine+userのPATHを読み込み直す必要がある場面があった（新規プロセスなら通常不要）。

---

以上を踏まえて、作業ブランチ `sub-branch-task14`（作成済み）上で、`plan_v0.2.md` のタスク1から作業を再開してください。
