# ChatHub Ver0.3 実装計画（タスク1: メタデータ取得の見直し）

本ドキュメントは、v0.2で保留にしたタスク1「OGP/メタタグからのタイトル取得可否の調査」を、
単発タスクではなく独立した実装計画として切り出したもの。`plan.md`（v0.0の記録）・
`plan_v0.1.md`（v0.1の記録）・`plan_v0.2.md`（v0.2の記録。タスク1除き完了・凍結済み）は
いずれも編集せず、本ファイルで v0.3（タスク1）の計画・進捗を管理する。

---

## 1. 背景・経緯

v0.1フェーズGで「Notionと同様に、タイトルだけであれば OGP/メタタグ情報（`<title>`, `og:title`等）
から取得できるのではないか」という仮説検証タスクが起票され、v0.2に持ち越された後、
「現状のスコープで着手するには難易度が高い」と判断され保留となった。

着手前に`src/app/api/metadata/route.ts`と`plan.md`5.6章・10.3章を確認したところ、
**この仮説の「タイトル」部分はv0.0の時点で既に実装済み**であることが判明した。
そのため、そのまま再着手しても得るものが少ない。以下2章で現状を正確に整理し、
3章で調査ゴールを再定義する。

## 2. 現状の実装整理（着手前に必ず確認すること）

`/api/metadata`（`src/app/api/metadata/route.ts`）は既に3段階のハイブリッド構成になっている。

```
フェーズ1: OGP直接fetch（タイトル用）─┐
                                      ├─ Promise.all で並行実行
フェーズ2: Browser Rendering（本文用）─┘
              │
              │ 失敗 / レンダリング未完了と判定された場合のみ
              ▼
フェーズ3: Jina Reader（本文のフォールバック、最大2回試行）
```

- **タイトル取得（フェーズ1・OGP直接fetch）は既にOGPベースで確定済み**。
  `fetchOgpMeta()`が`<title>`と`og:title`/`twitter:title`を比較し、ChatGPTだけ`<title>`を
  優先する等、3サービス分の実挙動差を吸収する分岐が既に入っている（`plan.md`5.6章に
  2026-09-15時点の実URL検証結果あり）。フェーズ2・3が失敗してもフェーズ1のタイトルは
  独立して返る設計。
- **本文（`contentText`、チャット全文）は3サービスいずれもOGP/メタタグには一切含まれず、
  JSレンダリング必須**であることが実URL検証で確認済み（`plan.md`174-175行目）。つまり
  「OGPタグ取得だけで済むページ」は、少なくともタイトルに関しては既に実現しており、
  本文に関しては原理的に存在しない（会話内容をサーバー側メタタグに埋め込んでいる
  AIサービスは無い）。
- Claudeは会話固有のタイトルがページのどこにも存在しないという「修正不可能な既知の限界」
  があり、Jinaが変換した本文の1行目（`## You said: ...`）を疑似タイトルとして使う次善策で
  対応済み（`extractHeadingTitle()`）。

**結論**: v0.1起票時点の「タイトルだけならOGPで済むのでは」という仮説は、正しかったが
既に実装済みであり、v0.3で新たに検証すべき論点ではない。真に手つかずなのは次章。

## 3. 再定義した調査ゴール

上記を踏まえ、本タスクの狙いを次のいずれか（または両方）に絞り直す。

### 3-1. 体感速度の改善（本命候補）
現状、アイテム保存時は**フェーズ1（速い）とフェーズ2（遅い、Browser Rendering）を
`Promise.all`で並行実行し、両方完了するまでレスポンスを返さない**。フェーズ2が失敗すると
フェーズ3（Jina Reader、最大30秒タイムアウト×2回リトライ＝最悪60秒近く）まで待つため、
「タイトルは一瞬で取れるはずなのに、保存ボタンを押してから本文取得の完了まで長時間
待たされる」というUXの機会損失がある。

**改善の方向性（案）**:
1. フェーズ1（OGPタイトル）が返り次第、タイトル確定＋アイテムを`content: null`の状態で
   即座に保存/表示する
2. フェーズ2・3（本文取得）は保存後にバックグラウンドで継続し、完了時にアイテムを
   `PATCH`で更新する（例: Cloudflare Queuesやwaitユニット、もしくはクライアント側から
   保存直後に非同期で`/api/metadata`本文取得→`/api/items/[id]`更新の2段階呼び出し）
3. UI側は「本文取得中…」のような状態表示が必要になる

この案は保存体験を大きく変えるアーキテクチャ変更になるため、実装するかどうかは
本ドキュメントで結論を出さず、次回着手時にユーザーと相談すること。

### 3-2. 実挙動の再検証（保険・低コスト）
`plan.md`5.6章の検証は2026-09-15時点のもの。ChatGPT/Claude/Geminiは共有ページの実装を
予告なく変更することがあるため、現在のOGP/`<title>`挙動が変わっていないか、実URLで
再確認しておく価値はある（特にChatGPTの「`<title>`優先」分岐や、Claudeの「タイトルが
どこにも無い」制限が今も変わっていないか）。ただし単独では優先度の低い確認作業であり、
3-1に着手する場合はその過程で自然に再検証されるため、別タスクとして独立させる必要は薄い。

## 4. 次回着手時の進め方

1. まず3-1（体感速度改善）に取り組む価値があるか、ユーザーに確認する
   （保存後に本文が遅れて反映される体験を許容できるか、実装コストに見合うか）
2. 着手する場合は、非同期化の実装方式（クライアント主導の2段階呼び出し / Cloudflare Queues等）
   を比較検討してから実装方針を1文で説明し、コードを提案する（`CLAUDE.md`の実装手順ルール）
3. 見送る場合は、タスク1自体をスコープ外として`plan_v0.3.md`を完結・凍結し、
   3-2の再検証のみ軽く実施して終了とする

ステータス凡例: `未着手` / `進行中` / `完了` / `保留`

## 5. ステータス

- タスク1（本文取得の非同期化 or 見送り判断）: 未着手

---

## 進捗ログ

（着手時にここへ日時・概要・テスト結果を追記する）

### 2026-09-18 起票
v0.2タスク1を保留にし、独立した実装計画として本ファイルを新設。現状の実装調査により、
タイトルのOGP取得は既にv0.0で実装済みであることが判明したため、調査ゴールを
「本文取得の体感速度改善（非同期化）」に再定義した（3章参照）。
