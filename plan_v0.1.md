# ChatHub Ver0.1 実装計画

本ドキュメントは v0.1 の要求事項を実装タスクに分解したロードマップ兼進捗管理表。
`plan.md`（v0.0の記録）は編集せず、本ファイルで v0.1 の計画・進捗を管理する。

進め方のルール:
- 1タスク = 1回の指示で実装・テスト完了できるサイズに分解する。
- タスク着手前に必ずステータスを「進行中」に更新し、完了後は「完了」＋簡単な実施内容・テスト結果を追記する。
- フェーズの順序は A → B → C → D → E → F → G。

ステータス凡例: `未着手` / `進行中` / `完了` / `保留`

---

## フェーズA: 単純バグ修正・調整

### タスク1: タグ追加バグの修正
- 内容: 新規作成・編集モーダルでタグが追加できない不具合を調査・修正する
- ステータス: 完了

### タスク2: 文字サイズ3段階UIの調整
- 内容: 現行「大」を「中」として再定義し、「小・中・大」の3段階をスムーズに切替・スライドできるUIを実装する（`FontSizeSetting.tsx`）
- ステータス: 未着手

---

## フェーズB: UI/デザイン微調整（まとめて実装）

### タスク3: UI/デザイン一括調整
- 内容:
  - 新規作成・編集時のトグルボタンを、新規作成ボタンと同じアクセントカラーの太線フレームで囲むデザインに変更
  - 詳細フィルター内の「★」アイコン・「お気に入りに登録」表記を、テーブル一覧の黄色い★アイコンに統一し「★お気に入り登録」表記に変更
  - ヘッダーの設定アイコン（歯車）の隣に「使い方ガイド」ボタン（🔰）を配置
  - 検索フィルター周辺の背景色を、全体の色彩に馴染む薄い補色に変更
  - 詳細フィルター内の各項目の余白（Margin/Padding）を広げる
  - フォルダ一覧等の「改名」「削除」テキストをアイコン化（改名: モノトーンのペンアイコン、削除: 小さめの赤色×アイコン）。一覧テーブルの操作列の削除ボタンも同様に統一
  - ファビコン画像の角を丸める
- ステータス: 未着手

---

## フェーズC: スクロール位置の復元

### タスク4: スクロールバック機能の実装
- 内容: 編集ボタン押下時に新規作成/編集領域へ自動スクロールする現行動作は維持しつつ、「保存」または「キャンセル」押下時に編集ボタンを押す前の元のスクロール位置へ正確に復元する処理を実装する
- ステータス: 未着手

---

## フェーズD: モバイル対応・サイドバー＆フォルダ操作の刷新

### タスク5: サイドバーリサイズ対応の現状確認・調整
- 内容: `ResizableSidebar.tsx` の現状を確認し、スマホ画面でのフォルダ名視認性・ユーザーによる幅/位置調整機能について不足分を調整する
- ステータス: 未着手

### タスク6: フォルダ並び替えのドラッグ＆ドロップ化
- 内容: 現行の▲▼ボタンによる並び替えUIを、ドラッグ＆ドロップによる並び替えに変更する
- ステータス: 未着手

### タスク7: フォルダソート選択肢の絞り込み
- 内容: フォルダソート機能の選択肢を「任意の順序」「名前順↑」「名前順↓」の3種類のみにし、「作成日順」を削除する
- ステータス: 未着手

---

## フェーズE: タグ・AIツールのカラーカスタマイズ機能

### タスク8: タグのカラーカスタマイズ
- 内容: タグごとにカラーを選択できるようにし、透明度が低め（薄く落ち着いたパステル調）のカラーパレットから選べるようにする。適切なデフォルトカラーを設定する
- ステータス: 未着手

### タスク8.5: AIツール管理機能の追加
- 内容: AIツールが現状 `Gemini/ChatGPT/Claude/DeepSeek` の固定プリセットのみで、新規作成/編集フォームで自由入力したAIツール名がそのアイテムにしか保存されず、ユーザー共通の一覧に永続化されないため、次回登録時に選択肢として残らない（再入力が必要）。タグ（`src/lib/kv/tags.ts`・`TagPanel.tsx`）と同様の仕組みをAIツールにも導入する:
  - ユーザーごとのAIツール一覧をKVに永続化（タグのensureTagsRegistered相当を新規作成/編集時に適用）
  - AIツール管理UI（改名・削除、削除時は付与済みアイテムからのカスケード削除も検討）を追加
  - タスク9（AIツールのカラーカスタマイズ）は、この管理された一覧を前提にするため本タスクの後に実施する
- ステータス: 未着手

### タスク9: AIツールのカラーカスタマイズ
- 内容: AIツールごとにカラーを選択できるようにし、透明度が高め（鮮やか・発色の良い）のカラーパレットから選べるようにする。適切なデフォルトカラーを設定する
- ステータス: 未着手

---

## フェーズF: 認証方式の刷新（パスキー＋マジックリンク化）

現状: Google/GitHub/X の OAuth + メールアドレス/パスワード認証（Supabase Auth、`src/app/login/page.tsx`）。
変更方針: サードパーティ連携＋パスキーを基本構成とし、パスキーを忘れた場合はメールアドレス宛のマジックリンクでログインできる構成に変更する。

### タスク10: パスキー（WebAuthn）対応状況の調査
- 内容: 現行 `@supabase/supabase-js@2.116` 系でのパスキーネイティブ対応状況を調査する。対応していない場合は代替手段（自前WebAuthn実装 or 他ライブラリ）を検討し、方針を決定する
- ステータス: 未着手

### タスク11: パスキー登録・ログインUIの実装
- 内容: 調査結果に基づき、OAuthボタンと並べてパスキー登録・ログインの導線を実装する
- ステータス: 未着手（タスク10の結果待ち）

### タスク12: メール+パスワード認証の廃止とマジックリンク導線への置き換え
- 内容: 現行のメールアドレス/パスワードログインフォームを廃止し、「パスキーを忘れた場合」の導線からマジックリンク送信フォームに置き換える
- ステータス: 未着手

### タスク13: 既存パスワード認証関連コードの整理
- 内容: `signInWithPassword` / `signUp` を使用している箇所の整理・移行、サーバー側 `src/lib/auth/verify.ts` 等への影響確認
- ステータス: 未着手

---

## フェーズG: 第0章メタデータ取得の仮説検証（全フェーズ完了後に着手）

### タスク14: OGP/メタタグからのタイトル取得可否の調査
- 内容: 「Notionと同様に、タイトルだけであれば OGP/メタタグ情報（`<title>`, `og:title` 等）から取得できるのではないか」という仮説を検証する。ChatGPT/Claude/Gemini の共有ページで実際に取得可能か調査し、現行のスクレイピング処理（Browser Rendering / Jina Reader）との比較検討を行う
- ステータス: 未着手

---

## スコープ外（今回対応しない項目）

- ブラウザ拡張機能化
- MCP/API化

---

## 進捗ログ

（各タスク完了時にここへ日時・概要・テスト結果を追記する）

### 2026-09-18 タスク1完了: タグ追加バグの修正
- 原因: `ItemForm.tsx` のタグチップ一覧が `availableTags`（既存の登録済みタグ一覧）のみを `.map()` して描画しており、`addCustomTag()` で `values.tags` に新規タグを追加すること自体は成功していたが、そのタグが `availableTags` に含まれていないため画面上に一切表示されなかった。ユーザーからは「タグが追加できない」ように見えていた。
- 修正: `availableTags` と `values.tags` の和集合（重複排除）を `tagOptions` として算出し、チップ一覧の描画元を `tagOptions` に変更（`src/components/items/ItemForm.tsx`）。
- テスト: ローカル環境にNode.js/npmが未導入だったため winget で Node.js LTS をインストールし、`npm install` を実施。認証まわりが必要になるため検証用の一時ページ（コミット対象外・作業後に削除）で `ItemForm` を単体レンダリングし、playwright-core（一時インストール、後で削除）経由でEdgeブラウザを操作して以下を確認:
  - 「追加」ボタンクリックで新規タグがチップとして表示される
  - Enterキーでも同様に追加される
  - 同名タグの重複追加は発生しない
  - 未確定のカスタムタグをトグルOFFにすると選択肢から消える（新規タグ用の暫定チップとして妥当な挙動）
  - 既存タグ（`availableTags`由来）のトグル選択は引き続き正常動作
  - `npm run lint`: エラーなし（既存警告3件は本修正と無関係）
  - `npm run build`: 成功

### 2026-09-18 タスク1追加修正: グローバルタグ一覧への自動登録
- 追加で見つかった原因: タグには「アイテムごとの`tags`配列」と「ユーザー共通のタグ一覧（`user:{userId}:tags`、プリセット選択肢用）」の2種類があり、新規作成/編集フォームでその場で入力したタグは前者にしか保存されず、後者には登録されなかった。保存後に一覧を再読み込みすると、そのタグはプリセットとして選べなくなり、次のアイテム作成時に「追加できない」ように見えていた。
- 修正: `src/lib/kv/tags.ts` に `ensureTagsRegistered()` を追加し、アイテムの新規作成（`src/app/api/items/route.ts`）・更新（`src/app/api/items/[id]/route.ts`）の両方で、保存時に使われたタグをユーザーのタグ一覧へ自動登録するように変更。
- 調査過程でのつまずきと学び: ローカル(`localhost:3000`)で検証していたはずが、実際にはブラウザのアドレスバー誤操作ではなく、**Supabaseダッシュボードの「許可されたリダイレクトURL」に`localhost:3000`が未登録だったため、OAuthログイン後に本番URL（`chathub-app.dev2logging.workers.dev`）へ自動フォールバックしていた**ことが判明。そのため一連の修正が本番環境に反映されておらず「直っていない」ように見えていた。ユーザーがSupabase側の設定にlocalhost:3000のリダイレクトURLを追加したことで解消。
- 最終確認: ユーザー本人が実際のログイン環境（localhost:3000）でタグ追加が正常に動作することを確認済み。
- `npm run lint` / `npm run build`: 問題なし。
