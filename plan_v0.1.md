# ChatHub Ver0.1 実装計画

本ドキュメントは v0.1 の要求事項を実装タスクに分解したロードマップ兼進捗管理表。
`plan.md`（v0.0の記録）は編集せず、本ファイルで v0.1 の計画・進捗を管理する。

進め方のルール:
- 1タスク = 1回の指示で実装・テスト完了できるサイズに分解する。
- タスク着手前に必ずステータスを「進行中」に更新し、完了後は「完了」＋簡単な実施内容・テスト結果を追記する。
- フェーズの順序は A → B → C → D → E → F → G。

ステータス凡例: `未着手` / `進行中` / `完了` / `保留`

---

## フェーズA: 単純バグ修正・調整

### タスク1: タグ追加バグの修正
- 内容: 新規作成・編集モーダルでタグが追加できない不具合を調査・修正する
- ステータス: 完了

### タスク2: 文字サイズ3段階UIの調整
- 内容: 現行「大」を「中」として再定義し、「小・中・大」の3段階をスムーズに切替・スライドできるUIを実装する（`FontSizeSetting.tsx`）
- ステータス: 完了
- 実施内容:
  - `globals.css`の`--font-scale`を再定義。旧「大」(1.25)→新「中」（デフォルト）、旧「中」(1.1)→新「小」、新「大」は1.4を新設
  - UIはスライダー式を一度実装したが、ユーザーから「小中大を選ぶだけなのでスライダーは逆にストレス」とフィードバックがあり、元のボタン式（3つの独立ボタン）に戻した
  - 切替時のfont-sizeトランジションも試したが「アニメーションもいらない」とのことで削除
  - デフォルト値が正しく「中」（属性なし＝`--font-scale: 1.25`）になっていることをユーザーと確認済み
  - `npm run lint` / `npm run build`: 問題なし
- 学び: 「スムーズに切替・スライドできるUI」という要求文言を字面通りスライダーUIと解釈したが、実際に求められていたのは数値の再定義のみで、UI操作方法自体は変更不要だった。UI変更を伴う要求は、先に軽い確認を挟むと手戻りを減らせる

---

## フェーズB: UI/デザイン微調整（1〜2項目ずつ区切って実施）

### タスク3-1: トグルボタンの枠色 + ★アイコン統一
- 内容:
  - 新規作成・編集時のトグルボタンを、新規作成ボタンと同じアクセントカラーの太線フレームで囲むデザインに変更
  - 詳細フィルター内の「★」アイコン・「お気に入りに登録」表記を、テーブル一覧の黄色い★アイコンに統一し「★お気に入り登録」表記に変更
- ステータス: 完了
- 実施内容:
  - 「トグルボタン」は新規登録ボタンで開く新規作成/編集フォーム自体を指すとユーザーに確認。フォームの外枠を`border-2 border-primary`（太いアクセントカラー枠）に変更
  - フォーム内「お気に入りに登録」チェックボックス、詳細フィルター内「★のみ」チェックボックスの両方を、テーブル一覧と同じ黄色い★/☆アイコン式のトグルボタンに変更
  - 表記は当初「★お気に入り登録」としたが、アイコンと文字が重複するとのフィードバックがあり「お気に入り登録」（★の文字なし）に修正
  - ついでに新規作成/編集フォーム・詳細フィルター双方の項目間の余白も拡大（タスク3-2の余白調整分を前倒しで実施）
  - `npm run lint` / `npm run build`: 問題なし

### タスク3-2: 検索フィルター周辺の背景色 + 絞り込み全解除ボタン
- 内容:
  - 検索フィルター周辺の背景色を、全体の色彩に馴染む薄い補色に変更
  - （項目間の余白調整はタスク3-1で前倒し実施済み）
  - 追加要望: 「絞り込み適用中」バッジがある場合、詳細フィルターの一番下に「絞り込み全解除」ボタンを設置する（グレー系の色調で、解除アクションであることが分かるように）
- ステータス: 完了
- 実施内容:
  - 「絞り込み全解除」ボタンを追加。`advancedActive`（絞り込み適用中）の時のみ表示し、クリックでタグ・AIツール・お気に入り・期間の各絞り込みを初期値に戻す（検索キーワード・並び替えは対象外）。「絞り込み適用中」バッジに寄せた丸ピル形状で`bg-muted`のグレー系配色
  - 背景色（薄いブラウン）は色味の調整を何度か試したが、最終的にユーザーから「背景色指定なしの元の状態に戻してほしい」との判断があり、`--filter-bg`関連の追加は全てロールバック。背景色変更自体は見送り
  - ついでに「AIツール管理」「タグ管理」パネル内の重複見出しテキストを削除（トグルボタンの表記と重複していたため）
  - `npm run lint` / `npm run build`: 問題なし
- 学び: 色味調整は微妙なニュアンス（明度・彩度・透明度）が伝わりにくく、何度かの往復が必要になった。今後同様の「背景色をいい感じに」系の要望は、具体的なカラーコードや参考画像を先に求めるとやり取りを減らせる可能性がある

### タスク3-3: 改名/削除のアイコン化 + favicon角丸
- 内容:
  - フォルダ一覧等の「改名」「削除」テキストをアイコン化（改名: モノトーンのペンアイコン、削除: 小さめの赤色×アイコン）。一覧テーブルの操作列の削除ボタンも同様に統一
  - ファビコン画像の角を丸める
- ステータス: 完了
- 実施内容:
  - `lucide-react`の`PencilIcon`/`XIcon`を使用し、タグ管理・AIツール管理・フォルダ一覧の「改名」→ペンアイコン、「削除」→赤色×アイコンに変更（`TagPanel.tsx`/`AiToolPanel.tsx`/`FolderPanel.tsx`）
  - 一覧テーブル（`ItemTable.tsx`）の削除ボタンも同様に赤色×アイコンに変更（編集ボタンは仕様通りテキストのまま維持）
  - `public/icon.svg`（base64埋め込みPNGラッパー）に`clipPath`で角丸（rx=220、キャンバス1254に対し約17.5%）を追加。キャッシュ破棄用バージョンクエリを`svg_override_1`→`svg_override_2`に更新（`manifest.ts`・`layout.tsx`）
  - `npm run lint` / `npm run build`: 問題なし。ユーザー確認済み「完璧です」

これでフェーズB（タスク3-1〜3-3）完了。

---

## フェーズC: スクロール位置の復元

### タスク4: スクロールバック機能の実装
- 内容: 編集ボタン押下時に新規作成/編集領域へ自動スクロールする現行動作は維持しつつ、「保存」または「キャンセル」押下時に編集ボタンを押す前の元のスクロール位置へ正確に復元する処理を実装する
- ステータス: 完了
- 実施内容:
  - 調査の結果、編集ボタン押下時の自動スクロールは実装されていなかったため新規実装。`itemFormRef`でフォーム領域を参照し、`editing.mode === "edit"`になった時に`scrollIntoView`
  - `savedScrollYRef`でフォームを開く直前の`window.scrollY`を記録し、保存/キャンセル（「+ 新規登録」→「閉じる」を含む）時に`closeEditingAndRestoreScroll()`で復元
  - 追加要望: 検索ボックスのプレースホルダーから「タグ」を削除し、実際の検索ロジック（`visibleItems`のquery絞り込み）からもタグを検索対象から除外
  - `npm run lint` / `npm run build`: 問題なし。ユーザー確認済み「いい感じです」

---

## フェーズD: モバイル対応・サイドバー＆フォルダ操作の刷新

### タスク5: サイドバーリサイズ対応の現状確認・調整
- 内容: `ResizableSidebar.tsx` の現状を確認し、スマホ画面でのフォルダ名視認性・ユーザーによる幅/位置調整機能について不足分を調整する
- ステータス: 完了
- 実施内容:
  - PC版(`<aside>`)は既に`ResizableSidebar`でドラッグリサイズ対応済みだったが、モバイル版(Sheetの引き出し)は`w-72`固定幅でリサイズ不可だったことがフォルダ名の見づらさの原因と判明
  - モバイルのSheetにも`ResizableSidebar`を適用。`SheetContent`の幅指定を`data-[side=left]:w-auto`等でオーバーライドし、内容に応じて可変にした
  - `ResizableSidebar`の`clamp()`を「640px」と「画面幅の90%」の小さい方を上限とするよう変更し、狭いスマホ画面でドラッグしても画面外にはみ出さないようにした
  - 検証用の一時ページ + Playwright（モバイル幅375px）で、画面端までドラッグしても画面外にはみ出さず、Sheetが閉じないことを確認
  - 追加要望: ヘッダー右上の「ログアウト」ボタンが右端に寄りすぎているという指摘 → 実際の原因はパディング不足ではなく、**モバイル幅(375px前後)でヘッダー右側のボタン群が単純に横幅に収まりきらず、はみ出して画面外に隠れていた**ことだった（実測54pxオーバー）。「エクスポート」「ログアウト」ボタンをアイコン付きにし、`sm`未満ではテキストを隠す対応で解消
  - 事故: モバイル幅検証のため`playwright-core`を一時インストールした際、ユーザーのdev serverが稼働中の状態で`npm install`を実行してしまい、Windowsのファイルロック（workerd.exe使用中）が原因でdev serverプロセスが落ちる事故が発生。ユーザーに`npm run dev`の再起動を依頼して復旧。**今後、検証目的でnpm installを行う際は、事前にdev serverが動いているか確認すること**
  - `npm run lint` / `npm run build`: 問題なし。ユーザー確認済み「問題ありません」

### タスク6: フォルダ並び替えのドラッグ＆ドロップ化
- 内容: 現行の▲▼ボタンによる並び替えUIを、ドラッグ＆ドロップによる並び替えに変更する
- ステータス: 完了
- 実施内容:
  - `FolderPanel.tsx`（「任意の順序」モード時のみ）、▲▼ボタンをフォルダ行右側のグリップアイコン（`GripVerticalIcon`）のドラッグハンドルに置き換え
  - 当初HTML5ネイティブドラッグ＆ドロップ（`draggable`/`onDragStart`/`onDragOver`/`onDrop`）で実装したが、ユーザーからモバイルで動かないとの報告があり原因調査。**ネイティブDnD APIはモバイルブラウザのタッチ操作では発火しない仕様上の制約**と判明し、Pointer Events（`pointerdown`/`pointermove`/`pointerup`/`pointercancel`）ベースの自前実装に置き換え。`window`にリスナーを張り、`document.elementFromPoint()` + `data-folder-row-id`属性でドロップ先を判定する方式。マウス・タッチ両対応
  - ドラッグ中のページスクロール誤爆を防ぐため、グリップハンドルに`touch-none`（`touch-action: none`）を付与
  - アイテムをフォルダへドラッグ＆ドロップして移動する既存機能（`draggedItemId`、ネイティブDnDのまま）は今回のスコープ外として変更せず
  - 追加要望: モバイルサイドバー右上の閉じる（✕）ボタンが小さいとの指摘 → `src/components/ui/sheet.tsx`のボタンサイズを`icon-sm`(28px)→`icon`(32px)、アイコンも`size-5`(20px)に拡大。このSheetはサイドバー1箇所にしか使われていないため影響範囲は限定的
  - `npm run lint` / `npm run build`: 問題なし。ユーザーが実機（モバイル）で動作確認し「問題ないです」と確認済み
- 学び: 並び替え・移動系のドラッグ＆ドロップ機能はPC検証だけでは不十分で、**モバイル実機での動作確認が必須**。HTML5ネイティブDnD APIを新規に使う際は、モバイル対応が必要な画面では最初からPointer Events方式を検討した方が手戻りが少ない

### タスク7: フォルダソート選択肢の絞り込み
- 内容: フォルダソート機能の選択肢を「任意の順序」「名前順↑」「名前順↓」の3種類のみにし、「作成日順」を削除する
- ステータス: 完了
- 実施内容:
  - `FolderPanel.tsx`の`FolderSortMode`型を`"custom" | "name" | "created-desc" | "created-asc"`から`"custom" | "name-asc" | "name-desc"`に変更し、作成日順の2択を廃止
  - 名前順の降順（`name-desc`）を新設し、プルダウンを「任意の順序 / 名前順↑ / 名前順↓」の3択に変更
  - `folderSortMode`はローカルstate（`page.tsx`）のみで永続化されていなかったため、既存データの移行対応は不要だった
  - `npm run lint` / `npm run build`: 問題なし。ユーザー確認済み「完璧です」

これでフェーズD（タスク6〜7）完了。

---

## フェーズE: タグ・AIツールのカラーカスタマイズ機能

### タスク8: タグのカラーカスタマイズ
- 内容: タグごとにカラーを選択できるようにし、透明度が低め（薄く落ち着いたパステル調）のカラーパレットから選べるようにする。適切なデフォルトカラーを設定する
- ステータス: 未着手

### タスク8.5: AIツール管理機能の追加
- 内容: AIツールが現状 `Gemini/ChatGPT/Claude/DeepSeek` の固定プリセットのみで、新規作成/編集フォームで自由入力したAIツール名がそのアイテムにしか保存されず、ユーザー共通の一覧に永続化されないため、次回登録時に選択肢として残らない（再入力が必要）。タグ（`src/lib/kv/tags.ts`・`TagPanel.tsx`）と同様の仕組みをAIツールにも導入する:
  - ユーザーごとのAIツール一覧をKVに永続化（タグのensureTagsRegistered相当を新規作成/編集時に適用）
  - AIツール管理UI（改名・削除、削除時は付与済みアイテムからのカスケード削除も検討）を追加
  - タスク9（AIツールのカラーカスタマイズ）は、この管理された一覧を前提にするため本タスクの後に実施する
- ステータス: 完了
- 実施内容:
  - `src/lib/kv/aiTools.ts` を新設（`tags.ts`と同構造）。ユーザーごとのAIツール一覧をKVに永続化し、デフォルトはGemini/ChatGPT/Claude/DeepSeekを遅延シード
  - `src/app/api/ai-tools/route.ts`（GET/POST/PATCH改名/DELETE）を新設
  - アイテム新規作成・更新時（`src/app/api/items/route.ts`・`src/app/api/items/[id]/route.ts`）に、使われたAIツールを自動登録
  - 削除はタグと異なり「選択肢一覧から外すのみ」とし、既存アイテムのaiTool値自体は変更しない設計にした（単一の自由記述フィールドであり、カスケードで既存表示を消すと意図せぬデータ変化に見えるため）
  - アカウント削除フロー（`src/app/api/account/route.ts`）にも`deleteAllAiTools`を追加
  - `AiToolPanel.tsx`（`TagPanel.tsx`と同構造）を新設し、サイドバーに「AIツール管理を開く」を「タグ管理を開く」の直前（追加要望通り）に配置
  - `ItemForm.tsx`のAIツールボタン一覧を固定プリセットから永続化リスト（+入力中の値）に変更し、一度使ったカスタムAIツールが次回以降も選択肢に残るようにした
  - 追加要望: サイドバーの「すべて」「未分類」を「フォルダ」見出し＋並び替えプルダウンより上に移動（`FolderPanel.tsx`）
  - `npm run lint` / `npm run build`: 問題なし。ユーザー本人が実機で動作確認し「問題ありません」と確認済み
- 見送り事項: 「タグ削除がアイテム一覧に反映されない」不具合の調査は、ユーザー確認の結果「必要なさそう」とのことでスキップ

### タスク9: AIツールのカラーカスタマイズ
- 内容: AIツールごとにカラーを選択できるようにし、透明度が高め（鮮やか・発色の良い）のカラーパレットから選べるようにする。適切なデフォルトカラーを設定する
- ステータス: 未着手

---

## フェーズF: 認証方式の刷新（パスキー＋マジックリンク化）

現状: Google/GitHub/X の OAuth + メールアドレス/パスワード認証（Supabase Auth、`src/app/login/page.tsx`）。
変更方針: サードパーティ連携＋パスキーを基本構成とし、パスキーを忘れた場合はメールアドレス宛のマジックリンクでログインできる構成に変更する。

### タスク10: パスキー（WebAuthn）対応状況の調査
- 内容: 現行 `@supabase/supabase-js@2.116` 系でのパスキーネイティブ対応状況を調査する。対応していない場合は代替手段（自前WebAuthn実装 or 他ライブラリ）を検討し、方針を決定する
- ステータス: 未着手

### タスク11: パスキー登録・ログインUIの実装
- 内容: 調査結果に基づき、OAuthボタンと並べてパスキー登録・ログインの導線を実装する
- ステータス: 未着手（タスク10の結果待ち）

### タスク12: メール+パスワード認証の廃止とマジックリンク導線への置き換え
- 内容: 現行のメールアドレス/パスワードログインフォームを廃止し、「パスキーを忘れた場合」の導線からマジックリンク送信フォームに置き換える
- ステータス: 未着手

### タスク13: 既存パスワード認証関連コードの整理
- 内容: `signInWithPassword` / `signUp` を使用している箇所の整理・移行、サーバー側 `src/lib/auth/verify.ts` 等への影響確認
- ステータス: 未着手

---

## フェーズG: 第0章メタデータ取得の仮説検証（全フェーズ完了後に着手）

### タスク14: OGP/メタタグからのタイトル取得可否の調査
- 内容: 「Notionと同様に、タイトルだけであれば OGP/メタタグ情報（`<title>`, `og:title` 等）から取得できるのではないか」という仮説を検証する。ChatGPT/Claude/Gemini の共有ページで実際に取得可能か調査し、現行のスクレイピング処理（Browser Rendering / Jina Reader）との比較検討を行う
- ステータス: 未着手

---

## スコープ外（今回対応しない項目）

- ブラウザ拡張機能化
- MCP/API化
- 使い方ガイドボタンの設置（保留）: 将来的にはボタン押下でLP（使い方ガイド）ページへ遷移させる想定だが、そのLPサイト自体が未作成のため、ボタン設置自体を今回は見送る。LPサイトが用意できた段階で改めてタスク化する

---

## 進捗ログ

（各タスク完了時にここへ日時・概要・テスト結果を追記する）

### 2026-09-18 タスク1完了: タグ追加バグの修正
- 原因: `ItemForm.tsx` のタグチップ一覧が `availableTags`（既存の登録済みタグ一覧）のみを `.map()` して描画しており、`addCustomTag()` で `values.tags` に新規タグを追加すること自体は成功していたが、そのタグが `availableTags` に含まれていないため画面上に一切表示されなかった。ユーザーからは「タグが追加できない」ように見えていた。
- 修正: `availableTags` と `values.tags` の和集合（重複排除）を `tagOptions` として算出し、チップ一覧の描画元を `tagOptions` に変更（`src/components/items/ItemForm.tsx`）。
- テスト: ローカル環境にNode.js/npmが未導入だったため winget で Node.js LTS をインストールし、`npm install` を実施。認証まわりが必要になるため検証用の一時ページ（コミット対象外・作業後に削除）で `ItemForm` を単体レンダリングし、playwright-core（一時インストール、後で削除）経由でEdgeブラウザを操作して以下を確認:
  - 「追加」ボタンクリックで新規タグがチップとして表示される
  - Enterキーでも同様に追加される
  - 同名タグの重複追加は発生しない
  - 未確定のカスタムタグをトグルOFFにすると選択肢から消える（新規タグ用の暫定チップとして妥当な挙動）
  - 既存タグ（`availableTags`由来）のトグル選択は引き続き正常動作
  - `npm run lint`: エラーなし（既存警告3件は本修正と無関係）
  - `npm run build`: 成功

### 2026-09-18 タスク1追加修正: グローバルタグ一覧への自動登録
- 追加で見つかった原因: タグには「アイテムごとの`tags`配列」と「ユーザー共通のタグ一覧（`user:{userId}:tags`、プリセット選択肢用）」の2種類があり、新規作成/編集フォームでその場で入力したタグは前者にしか保存されず、後者には登録されなかった。保存後に一覧を再読み込みすると、そのタグはプリセットとして選べなくなり、次のアイテム作成時に「追加できない」ように見えていた。
- 修正: `src/lib/kv/tags.ts` に `ensureTagsRegistered()` を追加し、アイテムの新規作成（`src/app/api/items/route.ts`）・更新（`src/app/api/items/[id]/route.ts`）の両方で、保存時に使われたタグをユーザーのタグ一覧へ自動登録するように変更。
- 調査過程でのつまずきと学び: ローカル(`localhost:3000`)で検証していたはずが、実際にはブラウザのアドレスバー誤操作ではなく、**Supabaseダッシュボードの「許可されたリダイレクトURL」に`localhost:3000`が未登録だったため、OAuthログイン後に本番URL（`chathub-app.dev2logging.workers.dev`）へ自動フォールバックしていた**ことが判明。そのため一連の修正が本番環境に反映されておらず「直っていない」ように見えていた。ユーザーがSupabase側の設定にlocalhost:3000のリダイレクトURLを追加したことで解消。
- 最終確認: ユーザー本人が実際のログイン環境（localhost:3000）でタグ追加が正常に動作することを確認済み。
- `npm run lint` / `npm run build`: 問題なし。
