# Claude Code スキル一覧

作成日: 2026-09-16
※ このセッション（VSCode拡張版 Claude Code）で `Skill` ツール経由で呼び出せるスキルの一覧です。
　 `/スキル名` の形で明示的に呼び出せるほか、該当するタスクだと判断した場合は自動的に使われることもあります。
　 利用可能なスキルは環境（プロジェクトやプラン）によって変わる場合があるため、定期的に見直してください。

## 一覧表

| スキル名 | 概要 | 主な用途・使いどころ |
|---|---|---|
| **design** | デザインキャンバスを作成し、Artifactとして公開する。複数のアートボードをパン/ズーム可能な1枚のキャンバスに配置し、クリック選択やインライン編集で後から視覚的に調整できる。 | UIモックアップ、画面遷移、ランディングページ、ポスター・チラシ・バナーなどのビジュアルデザインを作りたいとき |
| **dataviz** | チャート・グラフ・ダッシュボードなど、あらゆるデータ可視化を作る前に必ず読み込むべきスキル。配色ルール・レイアウト指針・アクセシビリティを揃えたデザインシステムを提供。 | HTML/React Artifact、SVG、matplotlib/plotly/d3/Rechartsなどでグラフや統計タイルを作る前 |
| **artifact-design** | Artifact全般のデザイン基本方針。Markdownファイルを書く場合でも公開前に必ず参照する。 | 何らかのArtifactを公開する前の下準備 |
| **artifact-diagramming** | Artifact内で図（ダイアグラム）を描く際のノウハウ。インラインSVGでライト/ダーク両テーマに対応した見やすい図を作る方法。 | 仕組みや構造を図で説明したいとき |
| **artifact-capabilities** | Artifactに「実行時の機能」（ライブデータ取得、状態の保存、共有状態、投票・チェックリストなど）を持たせるための仕様。 | インタラクティブ/永続化機能付きのArtifactを作る前 |
| **update-config** | Claude Codeの `settings.json`（フック、権限、環境変数など）を設定する。「〜のたびに」「毎回」といった自動化はフックでのみ実現可能。 | 権限追加、フック設定、環境変数変更などの依頼時 |
| **keybindings-help** | `~/.claude/keybindings.json` を編集し、キーボードショートカットをカスタマイズする。 | ショートカットの再割り当てやチョード（複合キー）追加時 |
| **code-review** | 現在の差分やPR番号・ブランチを対象に、正確性のバグや簡略化・効率化の指摘をレビューする。`--fix`で自動修正、`--comment`でPRにコメント投稿も可能。 | コードレビューを依頼されたとき |
| **simplify** | 変更したコードの再利用性・簡潔さ・効率を見直し、実際に修正まで適用する（バグ探しはしない）。 | コード品質の改善・リファクタリングをしたいとき |
| **fewer-permission-prompts** | セッション履歴から頻出するBash/MCPツール呼び出しを分析し、プロジェクトの`.claude/settings.json`に許可リストを追加して権限確認の頻度を減らす。 | 同じ許可を何度も聞かれて煩わしいとき |
| **loop** | プロンプトやスラッシュコマンドを一定間隔で繰り返し実行する（間隔省略でAIが自己ペース調整）。 | 「5分ごとに確認して」など定期実行タスクを設定したいとき |
| **schedule** | cronスケジュールで動くクラウド上の定期実行エージェント（ルーティン）を作成・管理する。 | 定期実行や1回限りの予約実行（例: 明日の朝リマインド）を設定したいとき |
| **claude-api** | Claude API / Anthropic SDKのリファレンス（モデルID、料金、パラメータ、ストリーミング、ツール使用、MCP、プロンプトキャッシュなど）。 | Claude/Anthropicが絡む実装や、LLM関連の質問全般（他社LLM明示時を除く） |
| **run** | プロジェクトのアプリを実際に起動・操作して変更を確認する。CLI/サーバー/TUI/Electron/ブラウザ等、プロジェクトの種類に応じた起動方法を判断。 | 「アプリを起動して」「動作確認して」と言われたとき |
| **init** | コードベースを解析し、新規に `CLAUDE.md`（プロジェクト説明ドキュメント）を初期化する。 | 新しいプロジェクトでCLAUDE.mdをまだ作っていないとき |
| **security-review** | 現在のブランチにある未コミットの変更を対象にセキュリティレビューを実施する。 | セキュリティ観点でのレビューを依頼されたとき |

## 補足

- 上記はあくまで**現在のセッションで使えるスキル**の一覧です。プラグインや環境設定によって増減します。
- `/loop` や `/schedule` は「今すぐ実行」ではなく「繰り返し・予約実行の設定」を行うスキルなので、日常のワンオフ作業には使わない。
- `code-review` と `security-review` は目的が異なる（前者はバグ・品質、後者はセキュリティ）ため、依頼内容に応じて使い分ける。
- `dataviz` と `artifact-design` はどちらも「Artifactを書く前に読む」設計だが、`dataviz` はグラフ専用、`artifact-design` は全Artifact共通の下地。
